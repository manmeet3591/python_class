各種データセットからsigmaファイルをつくるシェルスクリプト

主な設定はcinfigureを編集。細かな設定はスクリプトを参照。

どのスクリプト（データセット）から作ったかを区別するために
sigmaファイルのusridに値を入れるようにしてある。IDはスクリプト
の２行目に書いてあるSCRIPT IDで区別する（namelistのusrid）。
何も設定しない場合は99が入る。

後で区別できなくなるのでIDはスクリプトごとに設定するのをおすすめする。